<div id="left_col">
<% title "Viewing Photo: " + h(@photo.name) %>
<h1><%=h yield(:title) %></h1>
<p class="thumb">
<%= link_to image_tag(@photo.image.url(:small)), @photo.image.url, :class => "thickbox", :rel => h(@photo.name) %>
</p>
</div>
<div id="sidebar">
 <h3><%= link_to "&#187; Back to Project Home", project_path(project_param) %></h3>
 <%= render :partial => "projects/project_sidebar"  %>
</div>